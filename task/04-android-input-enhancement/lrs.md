任务：
当前分析按钮是硬编码，现在需要改成可以真实输入的按钮。支持语音输入和文字输入。接受到server传回的文字需要文字转语音输出。（语音转文字和文字转语音Android端都有自带的功能）

预期效果：
1、把“分析”按钮改成一个稍微大一点的小窗口，只要可以容纳一个语音键、一个文本框、一个发送按钮就行。用户可以语音输入、文本输入，最后发送。但是语音输入需要先回填到文本框，最后用户再确认发送。也就是最后发送的内容只有文本框里的问题。
2、发送问题后这个小窗口就回退到现在的取消按钮就可以。